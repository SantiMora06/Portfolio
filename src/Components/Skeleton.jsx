
const Skeleton = ({ height, circle, mb, mt, radius, width }) => {


    return (<>
        <>
            <Skeleton height={height}
                circle={circle}
                mb={mb}
                mt={mt}
                radius={radius}
                width={width} />
        </>

    </>);
}

export default Skeleton;